name: casino_plus_app

description: カジノ+ アプリ

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: 1.0.0-dev.10
  hooks_riverpod: 1.0.0-dev.10
  riverpod: 1.0.0-dev.10
  intl:
  http:
  file_picker:
  idb_sqflite:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
