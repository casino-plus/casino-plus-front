import '../../types/api_models.dart' as model;
import '../../components/hook_widget.dart';

class BetBox extends HookWidget {
  @override
  Widget build(BuildContext context) {
    return Text('BetBox');
  }
}
